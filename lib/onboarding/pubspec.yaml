name: onboarding
description: onboarding module for app
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

dev_dependencies:
  build_runner:

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


module:
  androidX: true
  androidPackage: com.example.myapp.onboarding
  iosBundleIdentifier: com.example.myapp.onboarding
