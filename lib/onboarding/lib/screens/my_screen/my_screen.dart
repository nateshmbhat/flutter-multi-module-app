// @author nateshmbhat created on 05,August,2020

import 'package:flutter/material.dart';

class MyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Change this Text string in code. It will not hot reload or hot restart.'),
      ),
    );
  }
}
